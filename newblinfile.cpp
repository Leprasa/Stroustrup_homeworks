#include <iostream>
int main()
{
        int m[10] = {1, 2, 3};
        char f[3] = {'фцува', 'цуАцуа', 'цуАЦуа'};
        char s = 'a';
        char* p = &c; // указатель на символ
        int* p = m; // указатель на массив
        char* p = f; //указатель на массив символьных строк
        char** p = s; //указатель на указатель на символ
        const c = 1;
        const int* p = c; //указатель на константу
        int* const p = c; //константный указатель
}